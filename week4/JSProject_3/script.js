  

  var numArray = [

    [] = prompt("Enter a number"),
    [] = prompt("Enter another number"),
    [] = prompt("Enter a number again"),
  ];

function sumArray(numArray){
  var i = 0;
  for(i=0; i < 3; i++){
    }
    var sum = 0;
    sum += parseInt(numArray[i], 10);
    return this.sum;
  }

        document.getElementById("demo").style.fontSize = "96px";
        document.getElementById("demo").innerHTML = "By the way the sum of your numbers is " + sumArray(numArray) + ".";
